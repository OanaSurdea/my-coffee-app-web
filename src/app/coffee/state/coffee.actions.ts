import { createAction } from '@ngrx/store';

export const sortByDateCreated = createAction('[CoffeeList Component] Sort list by dateCreated');
export const sortByRating = createAction('[CoffeeList Component] Sort list by rating');
